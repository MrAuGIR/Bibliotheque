<div class="tags-line">
  {% for tag in tags %}
  {% set color = tag.color is defined ? tag.color : '#c51919'%}
  {% set code = tag.code is defined ? tag.code : tag %}
  <span class="tag" style="background-color: {{color}};">{{code}}</span>
  {% endfor %}
</div>
