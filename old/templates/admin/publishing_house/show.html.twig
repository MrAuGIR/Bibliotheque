{% extends 'base.html.twig' %}

{% block title %}PublishingHouse{% endblock %}

{% block body %}
    <h1>PublishingHouse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publishing_house.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ publishing_house.name }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ publishing_house.country }}</td>
            </tr>
            <tr>
                <th>Info</th>
                <td>{{ publishing_house.info }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('publishing_house_index') }}">back to list</a>

    <a href="{{ path('publishing_house_edit', {'id': publishing_house.id}) }}">edit</a>

    {{ include('publishing_house/_delete_form.html.twig') }}
{% endblock %}
