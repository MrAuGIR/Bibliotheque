{% extends 'base.html.twig' %}

{% block title %}Cover index{% endblock %}

{% block body %}
    <h1>Cover index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>FileName</th>
                <th>PathFile</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cover in covers %}
            <tr>
                <td>{{ cover.id }}</td>
                <td>{{ cover.fileName }}</td>
                <td>{{ cover.pathFile }}</td>
                <td>
                    <a href="{{ path('cover_show', {'id': cover.id}) }}">show</a>
                    <a href="{{ path('cover_edit', {'id': cover.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('cover_new') }}">Create new</a>
{% endblock %}
